pre-commit:
  parallel: true
  commands:
    eslint:
      glob: '*.{ts,tsx,js,jsx}'
      run: npm run lint
    format:
      glob: '*.{js,jsx,ts,tsx}'
      run: npm run format -- {staged_files} && git add {staged_files}
    type-check:
      run: npm run type-check